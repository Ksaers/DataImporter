CREATE TABLE users (
                           id SERIAL PRIMARY KEY,
                           name VARCHAR(50) NOT NULL,
                           age INTEGER NOT NULL,
                           city VARCHAR(50) NOT NULL
);
